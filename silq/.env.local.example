# Build-time only. Never prefixed NEXT_PUBLIC_, never imported by client code,
# never sent to the browser. Used solely by `npm run images`.
#
# Get a free key (instant, no card) at https://www.pexels.com/api/
# Then copy this file to .env.local and paste the key in.
PEXELS_API_KEY=
