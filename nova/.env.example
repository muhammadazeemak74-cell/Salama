# Copy to .env.local and fill in. NEVER commit real keys.

# Unsplash Access Key — used SERVER-SIDE only to fetch licensed section
# photography. Get one at https://unsplash.com/developers.
# If unset, the site falls back gracefully to brand-gradient cards.
UNSPLASH_ACCESS_KEY=
