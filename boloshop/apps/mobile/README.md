# BoloShop Mobile

Flutter app (iOS / Android). Not yet scaffolded — run `flutter create .` here to
generate the project, then commit the result.

Mobile-first for Pakistan: assume low bandwidth, and consume live video via HLS
adaptive streaming from `services/media-service`.
