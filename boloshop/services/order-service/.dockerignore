# Build context for the order-service image is this directory.

.git
.gitignore

# Compiled inside the image.
bin
dist

# Never ship real credentials into a layer.
.env
.env.*
!.env.example

*.log
Dockerfile
.dockerignore
